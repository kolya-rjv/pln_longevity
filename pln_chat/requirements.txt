gradio>=4.0
openai>=1.0
pydantic>=2.0
python-dotenv>=1.0
pypdf>=4.0          # PDF text extraction for the Ontology Expander pipeline
# PLN runtime: the calibration + deduction layers are verified against this
# exact version (see pln_deduction.metta §5 / docs/deduction_layer.md).
hyperon==0.2.10
